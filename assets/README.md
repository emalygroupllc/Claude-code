# Assets

Drop the transparent-background EMALY GROUP logo here as `logo.png`.

The site references `assets/logo.png` in the nav and footer. Until the file
exists, a styled "E" emblem fallback renders automatically — no code change
needed when you add the real file.
